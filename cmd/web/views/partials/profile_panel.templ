package partials

import "sd/pkg/types"

templ ProfilePanel(instanceID string, deviceID string, profiles []types.Profile) {
	<div class="w-64 bg-sd-dark border-r border-sd-darker p-4">
		<h2 class="text-xl font-semibold mb-4">Profiles</h2>
		<div>
			//hx-ext="sse"
			//sse-connect={ "/stream/" + instanceID }
			//sse-swap="ProfileCardListUpdate"
			@ProfileCardList(instanceID, deviceID, profiles)
		</div>
	</div>
}
